# ParkVision — VS Code Setup Guide

See the full guide below. Quick start:

1. Open this folder in VS Code
2. Install recommended extensions when prompted
3. Create venv: `python -m venv .venv` then `pip install -r requirements.txt`
4. Press F5 → "Start Web Server"
5. Open http://localhost:5000

Full guide is in the README above.
